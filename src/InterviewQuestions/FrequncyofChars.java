/*
package InterviewQuestions;

public class FrequncyofChars {
    String str="aaabbccccdddee";
    int counter=0;
    String result="";

        for (int i = 0; i < str.length(); i++) {

        counter = 0;
        char ch=   str.charAt(i);
        for (int j = 0; j < str.length(); j++) {
            if (ch== str.charAt(j)) {
                counter++;


            }

        }
        if (!result.contains(ch+"")){
            result+=ch+": "+counter+ "\n";
        }System.out.println(result);

    }
}


*/
